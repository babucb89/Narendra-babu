#include<stdio.h>
int main()
{
int size;
printf("enter size of array");
scanf("%d",&size);
int a[size],i;

printf("Enter the elements %d",i);

for(i=0;i<size;i++)
{
scanf("%d",&a[i]);
{
}
int l=sizeof(a)/sizeof (a[i]);
printf("number of array is %d \n",l);

}

return 0;

}
